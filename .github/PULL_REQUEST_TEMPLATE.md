## What

Brief description of changes.

## Why

What problem does this solve?

## Checklist

- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] No hardcoded API keys
- [ ] English default, Korean in `lang === 'ko'` only
